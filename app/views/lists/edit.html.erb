<h1>編集画面</h1>

<%= form_with model: @list,url: list_path(@list.id), method: :patch do |f| %>
<!--省略すると、<%= form_with model: @list do |f| %>　になる-->

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>

<% end %>